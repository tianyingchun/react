Global show aysnc request callback message
==========

When to use
----------
- Centered at the top of the display and disappear automatically,
- it is a kind of don't interrupt the user operation way of lightweight tips.

